name: Fetch Model File List

on:
  workflow_dispatch:
    inputs:
      model_id:
        description: 'Hugging Face Model ID (e.g., SulphurAI/Sulphur-2-base)'
        required: true
        default: 'SulphurAI/Sulphur-2-base'

permissions:
  contents: write

jobs:
  fetch-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: true

      - uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install huggingface_hub
        run: pip install huggingface_hub

      - name: Generate file list
        run: python generate_file_list.py "${{ github.event.inputs.model_id }}"

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add file_list.md
          git commit -m "Add file list for ${{ github.event.inputs.model_id }}" || echo "No changes"
          git push
